/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# The Gradle wrapper (gradlew, gradlew.bat, gradle/wrapper/*) is intentionally
# committed - it pins the exact Gradle version for reproducible clone-and-build,
# matching the companion attacker project. (Flutter's stock template ignores it.)

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
